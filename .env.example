BASE_URL=https://bestprice.com.ua
NODE_ENV=stage
EXECUTION_MODE=sample
SAMPLE_SIZE=10
CRAWL_CONCURRENCY=3

# Set to true for full crawl smoke (OK + cart price match); CI uses contract-only mode
# CRAWL_SMOKE_STRICT=true
